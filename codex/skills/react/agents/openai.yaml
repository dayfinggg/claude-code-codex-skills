interface:
  display_name: "React"
  short_description: "Build reliable, accessible React applications"
  default_prompt: "Use $react to implement and verify this React change against the project's installed version and conventions."
