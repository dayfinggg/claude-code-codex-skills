interface:
  display_name: "React Runtime"
  short_description: "Own components, Hooks, rendering, and hydration"
  default_prompt: "Use $react for this component, Hook, state, Effect, rendering, SSR, or hydration task; use $nextjs instead for Next routing, caching, or revalidation."
