interface:
  display_name: "Delegate Work"
  short_description: "Agent delegation and PR coordination"
  default_prompt: "Use $delegate-work to divide this task into safe, independent work packets and integrate the results."
