interface:
  display_name: "Redis Engineering"
  short_description: "Engineer Redis keys, TTLs, and failure semantics"
  default_prompt: "Use $redis to design, review, debug, or migrate this Redis-specific behavior with exact client and topology semantics."
