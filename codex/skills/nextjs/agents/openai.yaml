interface:
  display_name: "Next.js Framework"
  short_description: "Own Next routing, rendering, caching, and runtimes"
  default_prompt: "Use $nextjs for this installed-version Next router, rendering, cache, revalidation, handler, action, metadata, or runtime task; do not use it for generic React behavior."
